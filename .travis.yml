# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

r_github_packages:
  - vimc/orderly
  - vimc/orderlyweb@vimc-3103

addons:
  apt:
    packages:
      - libapparmor-dev
